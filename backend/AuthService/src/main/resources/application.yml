spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/zauthdb
    username: root
    password: Realme6G90T

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
